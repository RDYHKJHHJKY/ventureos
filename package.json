{
  "name": "ventureos",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/run-tests.mjs",
    "test:auth": "node scripts/validate-auth-endpoints.mjs",
    "test:deploy": "node scripts/validate-deploy-endpoints.mjs",
    "test:deploy:all": "node scripts/validate-deploy-targets.mjs",
    "test:smoke": "node scripts/validate-smoke-all.mjs",
    "test:ci": "npm run test && npm run test:auth && npm run build",
    "start": "node server.js",
    "seed:demo": "node scripts/seed-demo.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.0",
    "pg": "^8.11.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.1.0"
  }
}
